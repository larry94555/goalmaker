use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "change-this-before-exposing-searxng"
  limiter: false

search:
  safe_search: 1
  formats:
    - html
    - json
